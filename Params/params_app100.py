from Params.params import get_parameter


class GetRoomLiveUrl:
    """（001）001-课程相关-获取直播间链接"""
    params = get_parameter('getRoomLiveUrl')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetMyCourseList:
    """（002）002-课程相关-获取我的课程列表"""
    params = get_parameter('getMyCourseList')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class MyLiveCourse:
    """（004）004-课程相关-练习首页正在直播公开课和我的直播课程"""
    params = get_parameter('myLiveCourse')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class NewSendSms:
    """（005）005-课程相关-语音验证码"""
    params = get_parameter('newSendSms')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class ReturnMoneyShare:
    """（006）006-课程相关-获取推荐返现分享信息"""
    params = get_parameter('returnMoneyShare')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class IndexOpen:
    """（007）007-课程相关-首页最新公共课"""
    params = get_parameter('indexOpen')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GradeList:
    """（008）008-课程相关-班级列表"""
    params = get_parameter('gradeList')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class OpenList:
    """（009）009-课程相关-公开课"""
    params = get_parameter('openList')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetVieDeoWechat:
    """（010）010-课程相关-视频播放页面，班主任微信接口"""
    params = get_parameter('getVieDeoWechat')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class CourseList:
    """（011）011-课程相关-推荐课程（首页）"""
    params = get_parameter('courseList')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class CouponNum:
    """（012）012-课程相关-优惠券数量"""
    params = get_parameter('couponNum')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class YuYue:
    """（013）013-课程相关-课程预约"""
    params = get_parameter('YuYue')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class SetPlayLog:
    """（（015）015-课程相关-播放记录"""
    params = get_parameter('setPlayLog')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class UpdateToken:
    """（014）014-课程相关-我的课程-取得新的token"""
    params = get_parameter('updateToken')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class SetOrderTop:
    """（016）016-课程相关-课程置顶-删除/恢复"""
    params = get_parameter('setOrderTop')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class SetOfflinePlayLog:
    """（017）017-课程相关-视频观看日志记录"""
    params = get_parameter('setOfflinePlayLog')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class SetDownLog:
    """（018）018-课程相关-下载次数记录"""
    params = get_parameter('setDownLog')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class SetCourseDel:
    """（019）019-课程相关-订单删除/恢复"""
    params = get_parameter('setCourseDel')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class KeChengOpen:
    """（020）020-课程相关-所有公共课"""
    params = get_parameter('KeChengOpen')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class OfflineInfo:
    """（021）021-课程相关-购买线下-信息完善"""
    params = get_parameter('offlineInfo')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class KenChengInfo:
    """（022）022-课程相关-班级课程详情"""
    params = get_parameter('KenChengInfo')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GoldBuy:
    """（023）023-课程相关-金币购买课程"""
    params = get_parameter('goldBuy')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetSubject:
    """（024）024-课程相关-获取学科"""
    params = get_parameter('getSubject')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetRoomEnterUrl:
    """（025）025-课程相关-进入教室"""
    params = get_parameter('getRoomEnterUrl')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetRecommend:
    """（027）027-课程相关-视频播放-课程推荐"""
    params = get_parameter('getRecommend')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetRoll:
    """（026）026-课程相关-获取滚动数据"""
    params = get_parameter('getRoll')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetMyCourseInfo:
    """（028）028-课程相关-课程-视频列表"""
    params = get_parameter('getMyCourseInfo')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetListOne:
    """（029）029-课程相关-班级列表"""
    params = get_parameter('getListOne')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetList2:
    """（030）030-课程相关-课程目录"""
    params = get_parameter('getList2')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetList1:
    """（031）031-课程相关-课程列表-新版"""
    params = get_parameter('getList1')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetList0:
    """（032）032-课程相关-课程列表-新版1.7.0（推荐到首页）"""
    params = get_parameter('getList0')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetCoupon:
    """（033）033-课程相关-获取优惠券数据"""
    params = get_parameter('getCoupon')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetExpDetail:
    """（034）034-课程相关-获取物流详情"""
    params = get_parameter('getExpDetail')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class CheckPay:
    """（035）035-课程相关-是否已经支付"""
    params = get_parameter('checkPay')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class GetCharge:
    """（036）036-课程相关-ping++取得charge对象"""
    params = get_parameter('getCharge')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class TaskList:
    """（037）037-课程相关-任务中心"""
    params = get_parameter('taskList')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class SetSchoolTime:
    """（040）003-用户相关-设置学校、毕业时间"""
    params = get_parameter('setSchoolTime')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])


class MyOrder:
    """（041）004-用户相关-我的订单"""
    params = get_parameter('My_order')
    case_data = []
    for i in range(0, len(params)):
        case_data.append(params[i])
